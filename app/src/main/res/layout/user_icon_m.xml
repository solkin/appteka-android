<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <FrameLayout
        android:id="@+id/icon_back"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/user_icon_back"
        android:padding="6dp"
        tools:backgroundTint="@color/primary_color">

        <com.caverock.androidsvg.SVGImageView
            android:id="@+id/icon_svg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            tools:src="@drawable/ic_crown"
            tools:tint="@android:color/white" />

    </FrameLayout>

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/icon_image"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Appteka.Circle"
        tools:src="@color/primary_color"
        tools:visibility="gone" />

    <!-- ~46% of the 48dp avatar; mirrors user_icon_xl proportions. -->
    <FrameLayout
        android:id="@+id/badge_ring"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="bottom|end"
        android:background="@drawable/badge_overlay_ring"
        android:visibility="gone"
        tools:visibility="visible">

        <FrameLayout
            android:id="@+id/badge_back"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="1.5dp"
            android:background="@drawable/user_icon_back"
            android:padding="4dp"
            tools:backgroundTint="@color/primary_color">

            <com.caverock.androidsvg.SVGImageView
                android:id="@+id/badge_svg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                tools:src="@drawable/ic_crown"
                tools:tint="@android:color/white" />

        </FrameLayout>

    </FrameLayout>

</FrameLayout>
